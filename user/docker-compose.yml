version: '3'
services: 
  production-user:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4003:4003"
  development-user:
    build:
      context: .
      dockerfile: Dockerfile.dev