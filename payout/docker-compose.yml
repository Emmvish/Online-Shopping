version: '3'
services: 
  production-payout:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${PORT}:${PORT}"
      - "${MONGODB_PORT}:${MONGODB_PORT}"
    env_file:
      - ./config/config.env
  development-payout:
    build:
      context: .
      dockerfile: Dockerfile.dev